<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hs.server.dao.UserDAO" >
    <resultMap id="BaseResultMap" type="com.hs.server.domain.User" >
        <id column="userId" property="user_id" jdbcType="BIGINT" />
        <result column="userName" property="user_name" jdbcType="VARCHAR" />
        <result column="loginId" property="login_id" jdbcType="VARCHAR" />
        <result column="passWord" property="pass_word" jdbcType="VARCHAR" />
        <result column="role" property="role" jdbcType="BIGINT"/>
        <result column="userRight" property="user_right" jdbcType="BIGINT"/>
        <result column="createBy" property="create_by" jdbcType="VARCHAR"/>
        <result column="createDate" property="create_date" jdbcType="TIMESTAMP"/>
        <result column="updateBy" property="update_by" jdbcType="VARCHAR"/>
        <result column="updateDate" property="update_date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="BaseColumnList" >
        user_id, user_name, login_id, pass_word, role, user_right, create_by, create_date, update_by, update_date
    </sql>

    <select id="getUsers" parameterType="com.hs.server.domain.User" resultMap="BaseResultMap"  >
        SELECT
        <include refid="BaseColumnList" />
        FROM s_user
        WHERE 1 = 1
        <if test="userId != null and userId!=''"> AND user_id = #{userId}</if>
        <if test="userName != null and userName!=''"> AND user_name like concat('%',#{userName},'%')</if>
        <if test="loginId != null and loginId!=''"> AND login_id = #{loginId}</if>
        <if test="query != null and query!=''"> AND (login_id like concat('%',#{query},'%') or user_name like concat('%',#{query},'%'))</if>
    </select>

    <insert id="insert" parameterType="com.hs.server.domain.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO
        s_user
        (user_name, login_id, pass_word, role, user_right, create_by, create_date, update_by, update_date)
        VALUES
        (#{userName}, #{loginId}, #{passWord}, #{role}, #{userRight}, #{createBy}, #{createDate}, #{updateBy}, #{updateDate})
    </insert>

    <update id="update" parameterType="com.hs.server.domain.User" >
        UPDATE
        s_user
        SET user_name = #{userName}, pass_word = #{passWord}, role = #{role}, user_right = #{userRight}, update_by = #{updateBy}, update_date = #{updateDate}
        WHERE user_id = #{userId}
    </update>

    <delete id="delete" parameterType="java.lang.Long" >
        DELETE FROM
        s_user
        WHERE
        WHERE user_id = #{userId}
    </delete>
</mapper>